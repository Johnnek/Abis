Damit das Beispiel funktioniert, muss eine Datenbank xdb eingerichtet werden.
Das Einrichten der Datenbank ist im Skript beschrieben.

Falls Sie ein anderes Datenbanksystem als Apache Derby alias JavaDB verwenden, m�ssen
Sie Zeile 20 und 21 in KundeDAO anpassen. Falls Ihre Datenbank nicht xdb heisst, m�ssen
Sie Zeile 21 in KundeDAO anpassen.

Weiterhin m�ssen Sie den f�r die von Ihnen verwendete JDBC-Treiber zum Projekt hinzuf�gen.


in der workbench in sql:

use xdb;
create table Kunden(
KDNR Integer Primary Key,
Name Varchar(30),
KDGRP Integer)